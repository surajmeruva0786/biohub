# ---------------------------------------------------------------------------
# Competition dataset (Biohub - Cell Tracking During Development)
#
# NOT tracked in git. The archive is ~81.4 GB and the extracted tree is
# 81.59 GB across 24,886 files. GitHub rejects individual files over 100 MB
# and does not host repositories at this scale. The data is obtained from
# Kaggle instead -- see DATASET.md for the layout and verification procedure.
# ---------------------------------------------------------------------------
biohub-cell-tracking-during-development.zip
biohub-cell-tracking-during-development/

# Zarr / geff stores anywhere in the tree
*.zarr/
*.geff/

# Submissions and model artifacts
submission.csv
*.ckpt
*.pt
*.pth
*.onnx

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/

# OS
Thumbs.db
desktop.ini
.DS_Store
