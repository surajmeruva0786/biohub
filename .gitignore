# ---------------------------------------------------------------------------
# Competition dataset (Biohub - Cell Tracking During Development)
#
# NOT tracked in git. The archive is ~81.4 GB and the extracted tree is
# 81.59 GB across 24,886 files. No single file is large (max 5.91 MB), but
# GitHub does not host repositories at this total size. The data is obtained
# from Kaggle instead -- see DATASET.md for layout and verification.
# ---------------------------------------------------------------------------
biohub-cell-tracking-during-development.zip
biohub-cell-tracking-during-development/

# Zarr / geff stores anywhere in the tree
*.zarr/
*.geff/

# Submissions and model artifacts
submission.csv
*.ckpt
*.pt
*.pth
*.onnx

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/

# OS
Thumbs.db
desktop.ini
.DS_Store
reference/
